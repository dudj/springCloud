
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: test
  zipkin:
    base-url: http://120.79.193.21:9411/
  sleuth:
    sampler:
      percentage: 1
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 1000
    #默认的key就是方法名
    getProductInfoList:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
feign:
  hystrix:
    enabled: true
#management:
#  context-path: /
logging:
  level:
    org.springframework.cloud.netflix.feign: debug
