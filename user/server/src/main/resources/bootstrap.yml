
eureka:
  client:
    service-url:
      defaultZone: http://120.79.193.21:8080/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: user
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config
